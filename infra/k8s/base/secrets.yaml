# WARNING: Use sealed-secrets or external-secrets in production
# This is a template — do NOT commit with real values
apiVersion: v1
kind: Secret
metadata:
  name: scrape-secrets
  namespace: scrape-system
type: Opaque
stringData:
  redis-password: "CHANGE_ME"
  bull-board-password: "CHANGE_ME"
